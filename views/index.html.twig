<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <link href="css/bootstrap.min.css" rel="stylesheet" type="text/css">
        <link href="css/styles.css" rel="stylesheet" type="text/css">
        <title></title>
    </head>
    <body>
        <div class="container">
            <h1>Best Restaurants in Oregon</h1>
            <form action="/addCuisine" method="post">
                <label for="cuisine">Enter a type of cuisine:</label>
                <input class="form-control" type="text" name="cuisine">
                <button type="submit" class="btn">Add</button>
            </form>

            {% if cuisines is not empty %}

                {% for cuisine in cuisines %}
                <p><a href="/getCuisine/{{ cuisine.getId }}">{{ cuisine.getType }}</a> </p>
                {% endfor %}

                <form action="/deleteCuisines" method="post">
                    <button type="submit" class="btn">Delete Cuisines</button>
                </form>

            {% endif %}

        </div>
    </body>
</html>
